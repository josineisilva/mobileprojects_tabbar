import 'package:flutter/material.dart';

//
// Classe para a tela de aniversarios
//
class Birthdays extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Icon(
        Icons.cake,
        size: 120.0,
        color: Colors.orange,
      ),
    );
  }
}